{
  "name": "admin--api",
  "description": "",
  "version": "0.1.0",
  "dependencies": {
    "serverless": "3.7.4"
  },
  "devDependencies": {
    "serverless-domain-manager": "^6.0.2",
    "serverless-python-requirements": "^5.4.0"
  },
  "scripts": {
    "clean": "sls requirements clean && sls wsgi clean",
    "create-domain": "sls create_domain --verbose --stage",
    "deploy": "sls deploy --verbose --stage"
  }
}
